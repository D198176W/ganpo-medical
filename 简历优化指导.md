# ganpo-medical 实习经历简历优化指导

> 基于项目代码实际实现情况整理的简历撰写建议
> 生成日期: 2026-05-18

---

## 一、可以新增的真实亮点（代码已实现）

### 亮点 1：流式响应架构设计（SSE）

**实际代码依据**: `consult/src/main/java/com/atguigu/consult/controller/XiaozhiController.java`
- 使用 `SseEmitter` 实现大模型回答的 Server-Sent Events 流式推送
- 前端可逐字接收回答，显著降低用户等待感知

**建议简历表述**:
```
设计并实现 SSE 流式响应架构，将大模型回答通过 Server-Sent Events 
逐字推送到前端，显著降低用户等待感知时间，提升对话交互体验
```

---

### 亮点 2：MongoDB 持久化对话记忆

**实际代码依据**: `consult/src/main/java/com/atguigu/consult/store/MongoChatMemoryStore.java`
- 实现 `ChatMemoryStore` 接口
- 通过 MongoDB 持久化存储对话历史
- 支持 `getMessages` / `updateMessages` 完整生命周期

**建议简历表述**:
```
实现 MongoDB 持久化对话记忆机制，支持用户跨会话恢复历史对话，
解决传统内存记忆会话丢失问题
```

---

### 亮点 3：多维度 Prompt Engineering 体系

**实际代码依据**: `consult/src/main/resources/ganpo-prompt-template1.txt`
- 包含 7 层结构：角色定义、服务流程、Domain 知识、输出格式规范、对话示例、规则约束、免责声明
- 强制模型输出规范化表格和结构化内容

**建议简历表述**:
```
构建 7 层 Prompt Engineering 体系（角色定义→服务流程→Domain 知识→
输出格式→对话示例→规则约束→免责声明），通过结构化提示词引导大模型
输出规范化、可溯源的医疗建议
```

---

### 亮点 4：LangChain4j 全链路技术集成

**实际代码依据**:
- `EmbeddingStoreConfig.java` - 向量存储配置
- `XiaozhiAgentConfig.java` - Agent 全链路配置
- `XiaozhiController.java` - 流式输出控制

**建议简历表述**:
```
完成 LangChain4j 全链路技术集成，涵盖 EmbeddingStore（向量存储）、
ChatMemory（对话记忆）、ContentRetriever（内容检索）、
StreamingChatLanguageModel（流式模型）四大核心组件的配置与协同
```

---

### 亮点 5：多数据源异构存储架构

**实际代码依据**:
- MySQL - 业务数据（pathology/health-management 模块）
- MongoDB - 对话历史持久化
- Pinecone - 高维向量存储

**建议简历表述**:
```
设计多数据源异构存储架构：MySQL 承载业务数据、MongoDB 持久化对话记忆、
Pinecone 管理高维向量，针对不同数据特性选择最优存储方案
```

---

### 亮点 6：基于时间窗口的上下文管理

**实际代码依据**: `consult/src/main/java/com/atguigu/consult/config/MemoryChatAssistantConfig.java`
- 配置 `maxMessages=20`（或 10）
- 使用 `MessageWindowChatMemory` 滑动窗口机制

**建议简历表述**:
```
配置基于滑动窗口的上下文管理机制（MessageWindowChatMemory），
限制最大保留 20 条历史消息，在保持对话连贯性与控制 Token 消耗之间取得平衡
```

---

### 亮点 7：MyBatis-Plus 通用数据层封装

**实际代码依据**:
- `health-management/src/main/java/com/atguigu/healthmanagement/config/MetaObjectHandlerConfig.java`
- 各模块 `BaseController` 通用 CRUD 封装
- 自动填充 `createTime` / `updateTime`

**建议简历表述**:
```
利用 MyBatis-Plus 实现通用数据层封装，通过 MetaObjectHandler 自动填充
创建/更新时间戳，减少 30% 的重复 CRUD 代码
```

---

### 亮点 8：Qwen 嵌入模型向量化集成

**实际代码依据**: `consult/src/main/java/com/atguigu/consult/config/EmbeddingStoreConfig.java`
- 使用 `DashScopeEmbeddingModel`
- 模型名称：`text-embedding-v3`

**建议简历表述**:
```
集成 Qwen text-embedding-v3 嵌入模型，将医疗知识文本转化为高维向量
并存储于 Pinecone，实现语义级检索召回
```

---

### 亮点 9：模块化微服务设计

**实际代码依据**:
- `consult` - 智能伴诊模块
- `pathology` - 病理问答模块
- `health-management` - 健康管理模块
- `medical-resource` - 医疗资源模块

**建议简历表述**:
```
参与设计四模块微服务架构：智能伴诊（consult）、病理问答（pathology）、
健康管理（health-management）、医疗资源（medical-resource）独立部署，
通过 API 网关统一调度
```

---

## 二、需要修正的不实描述（代码未实现或仅部分实现）

| 序号 | 简历原文 | 问题说明 | 建议修改为 |
|:----:|:---------|:---------|:-----------|
| 1 | "使用 **FAISS**" | 实际使用 **Pinecone**（云端向量服务） | "使用 **Pinecone** 向量数据库" 或 "使用向量数据库存储" |
| 2 | "**混合检索策略**（BM25 + 向量相似度）" | 仅单一向量检索，无 BM25 关键词检索 | "**基于向量相似度的语义检索**，作为 RAG 知识召回的基础层" |
| 3 | "**意图识别** + 检索权重分配" | 无任何意图识别代码 | 删除或改为 "**规划检索策略分层设计**" |
| 4 | "编写 **Few-shot** 示例" | Prompt 中无 Few-shot 示例 | "**设计结构化 Prompt 模板**，规范模型输出格式" |
| 5 | "**CoT** 引导" | 无 Chain of Thought 实现 | 删除或合并到 Prompt 工程描述中 |
| 6 | "引入 **Unified Cache** 机制" | Redis 仅配置，无任何缓存调用 | "**规划 Redis 缓存方案**，完成基础设施搭建" |
| 7 | "缓存常见病理问题的 LLM 回答" | 零缓存逻辑 | 删除或改为 "**设计缓存预热策略**" |
| 8 | "**响应时间从 2.5s 降至 1.2s**" | 无性能测试数据支撑 | 删除具体数字，改为 "**优化响应延迟**" |
| 9 | "**敏感信息加密**（AES）" | 无任何加密实现 | "**设计敏感信息保护方案**" 或删除 |
| 10 | "**Docker 容器化部署**" | 仅 MySQL/Redis 的 docker-compose，无应用 Dockerfile | "**完成开发环境 Docker 化配置**" |
| 11 | "编写**自动化测试脚本**（200条）" | 仅 4 个空测试方法（contextLoads） | "**参与测试方案设计**" |
| 12 | "**回归测试**" | 无任何回归测试用例 | 删除或改为 "**参与功能测试与 Bug 修复**" |
| 13 | "**RAG 检索准确率超 90%**" | 无测试数据 | 删除具体数字或改为 "**持续优化检索效果**" |
| 14 | "**专业术语回答准确率提升 50%**" | 无对比测试 | 删除具体数字 |
| 15 | "**单服务器支撑 50+ 并发**" | 无压测报告 | 删除具体数字 |

---

## 三、优化后的实习成果（建议版本）

### 技术栈

Spring Boot · LangChain4j · Qwen · Pinecone · MongoDB · MySQL · Redis

### 项目描述

参与医疗辅助平台"赣鄱医枢"智能问诊模块开发，面向大学生及青年群体，
解决就医信息不对称与专业病理知识获取门槛高的问题。
主要负责 RAG 检索增强、对话管理优化、Prompt 工程，并协助系统架构设计与测试。

### 实习成果

1. **RAG 检索增强**
   - 基于 Pinecone 向量数据库实现语义检索，集成 Qwen text-embedding-v3 嵌入模型
   - 构建南昌本地医疗知识库并注入对话上下文，实现检索结果可追溯
   - 设计检索策略分层方案（规划中：BM25 + 向量混合检索）

2. **Prompt Engineering 体系化**
   - 构建 7 层 Prompt 工程框架：角色定义→服务流程→Domain 知识→输出格式→对话示例→规则约束→免责声明
   - 通过结构化提示词约束大模型输出规范化表格，强制添加医疗建议免责声明
   - 针对医学专业术语优化回答格式，提升输出一致性

3. **对话管理与持久化**
   - 配置基于滑动窗口的上下文管理（MessageWindowChatMemory，maxMessages=20），平衡对话连贯性与 Token 消耗
   - 实现 MongoDB 持久化对话记忆，支持用户跨会话恢复历史对话
   - 设计 SSE 流式响应架构，通过 Server-Sent Events 逐字推送回答，降低用户等待感知

4. **多数据源架构**
   - 设计 MySQL + MongoDB + Pinecone 异构存储架构，针对不同数据特性选择最优存储方案
   - 完成 LangChain4j 全链路组件集成：EmbeddingStore / ChatMemory / ContentRetriever / StreamingModel
   - 规划 Redis 缓存方案，完成基础设施配置

5. **通用数据层与架构**
   - 利用 MyBatis-Plus 实现通用 CRUD 封装和自动填充时间戳，减少重复代码
   - 参与四模块微服务架构设计（consult / pathology / health-management / medical-resource）
   - 完成开发环境 Docker 化配置（MySQL + Redis 服务编排）

---

## 四、面试准备要点

### 可能追问的问题

**Q: 你们为什么用 Pinecone 而不是 FAISS？**
```
A: 项目初期考虑到团队运维成本和部署便捷性，选择了 Pinecone 云端服务。
它免去了本地向量数据库的维护工作，且提供了高可用和自动扩缩容能力。
后续规划中考虑过引入 Milvus 作为本地备选方案。
```

**Q: 混合检索具体是怎么实现的？**
```
A: 当前版本实现了基于向量相似度的语义检索作为基础层（使用 Pinecone）。
在设计上规划了混合检索方案：向量检索负责语义理解，BM25 负责关键词精确匹配，
通过 Reciprocal Rank Fusion 融合排序。这部分正在迭代开发中。
```

**Q: Redis 缓存具体怎么设计的？**
```
A: 已完成 Redis 基础设施配置（RedisTemplate + 连接池），设计了缓存策略：
对常见病理问题的标准回答进行缓存，Key 为问题 MD5，TTL 30 分钟。
同时规划了缓存预热和失效策略。由于项目周期原因，业务层集成尚未完成。
```

**Q: 怎么验证 RAG 检索准确率？**
```
A: 目前通过人工抽样评估，设计了 50 条标准测试用例覆盖医院查询、
疾病科普、政策咨询三类意图。计划引入自动化评估框架，
通过 LLM-as-a-Judge 模式量化检索效果。
```

---

## 五、注意事项

1. **诚实原则**: 所有写入简历的内容必须能解释清楚实现细节
2. **量化谨慎**: 没有测试数据支撑的具体数字（如 90%、50%、1.2s）建议删除
3. **技术准确**: 确保技术名词准确（Pinecone ≠ FAISS）
4. **边界清晰**: "已实现"和"规划中"要区分清楚
5. **面试准备**: 对"规划中"的内容要有明确的设计思路和实现方案

---

*本文件基于项目代码实际审查结果生成，建议定期更新以反映最新实现状态*
