# .env.development（项目根目录）
# 开发环境 API 地址（本地后端服务）
# VITE_API_URL = "http://localhost:8083"
# 开发环境调试模式（开启日志）
VITE_DEBUG_MODE = true

# .env.development（开发环境）
VITE_APP_BASE_API = 'http://localhost:8082'  
# 后端接口基础路径，根据实际情况修改（如 http://localhost:8080/api）