# 浙江国科智舶科技有限公司 - Agent 应用开发工程师

> 2025.06 - 2025.08 | 南昌

---

## 技术栈

**后端框架**: Spring Boot 3.2 · MyBatis-Plus · Maven

**AI 技术栈**: LangChain4j 1.0 · Qwen (通义千问) · Prompt Engineering · RAG

**数据存储**: MySQL 8.0 · MongoDB · Redis · Pinecone · FAISS

**部署运维**: Docker · Docker Compose · Nginx

---

## 项目概述

参与医疗辅助平台**"赣鄱医枢"**智能问诊模块的核心开发，面向南昌地区大学生及青年群体，解决就医信息不对称、专业病理知识获取门槛高、三甲医院挂号流程复杂等痛点。在 3 人小组中担任后端核心开发，主导 RAG 检索增强、对话管理引擎、Prompt 工程体系建设，并推动系统性能优化与容器化部署。

---

## 核心职责与成果

### 一、RAG 检索增强系统（主要负责）

**问题背景**: 早期系统仅依赖大模型参数知识，存在医疗信息幻觉、时效性不足、本地化知识缺失等问题，用户关于南昌本地医院的咨询常得到错误指引。

**解决方案与实施**:

1. **双引擎混合检索架构**
   - **向量检索层**: 集成 Qwen text-embedding-v3 嵌入模型，将南昌 10+ 三甲医院科室信息、挂号指引、医保政策等医疗知识转化为 1536 维稠密向量，存储于 Pinecone 云端向量数据库，实现语义级召回
   - **关键词检索层**: 引入 FAISS 构建本地倒排索引，基于 BM25 算法实现关键词精确匹配，弥补向量检索对专业术语缩写、医院简称召回不足的缺陷（如"南大一附院"无法被向量语义理解）
   - **融合排序**: 采用 Reciprocal Rank Fusion (RRF) 算法融合两路检索结果，综合向量相似度得分与 BM25 词频得分，降低单一检索策略的偏置风险

2. **意图感知检索路由**
   - 设计三层意图分类器（规则引擎 + 关键词匹配 + 轻量分类模型），自动识别用户查询意图为**医院查询** / **疾病科普** / **政策咨询** / **症状自查**四类
   - 针对不同意图动态调整检索权重：医院查询类提升 BM25 权重（0.7:0.3），疾病科普类提升向量检索权重（0.3:0.7），政策咨询类采用均衡策略（0.5:0.5）
   - 通过 A/B 测试验证，混合检索在 50 条标准测试集上召回率较单一向量检索提升 23%

3. **检索效果量化验证**
   - 构建 200 条医学问答评估数据集（覆盖医院查询 60 条、疾病科普 80 条、政策咨询 40 条、症状自查 20 条），采用人工 + LLM-as-a-Judge 双轨评估机制
   - 经三轮迭代优化，RAG 检索准确率达到 92%（医院查询 95%、疾病科普 89%、政策咨询 91%）
   - 专业术语回答（如"弥漫性大 B 细胞淋巴瘤"）通过上下文注入强制引用知识库来源，幻觉率从通用模型的 18% 降至 4%

---

### 二、Prompt Engineering 与对话管理优化（主要负责）

**问题背景**: 早期提示词缺乏系统性设计，模型输出格式不统一、医学建议缺乏免责声明、复杂术语解释过于晦涩，存在合规风险。

**解决方案与实施**:

1. **七层 Prompt 工程体系**
   - 构建**角色定义→服务流程→Domain 知识→输出格式→Few-shot 示例→规则约束→免责声明**的七层结构化提示词框架
   - 在 Few-shot 层嵌入 12 组医学问答示例（涵盖常见症状、用药指导、就医建议），引导模型输出符合医疗行业规范的表格化回答
   - 在 CoT 引导层嵌入思维链指令："先分析症状→再匹配知识库→给出分级建议→提示就医紧迫性"，降低模型跳过推理直接给结论的风险

2. **对话记忆引擎优化**
   - 实现 **MongoDB 持久化对话记忆**（`MongoChatMemoryStore`），替代默认内存存储，支持用户跨会话恢复 7 天内的历史对话，解决会话重启后上下文丢失问题
   - 配置基于滑动窗口的上下文管理（`MessageWindowChatMemory`，`maxMessages=20`），在保持对话连贯性与控制 Token 消耗间取得平衡，单次对话平均 Token 消耗降低 35%
   - 实现用户级记忆隔离（`memoryId` 为唯一标识），杜绝用户 A 对话记录被用户 B 访问的数据泄露风险

3. **SSE 流式响应架构**
   - 设计并实现 Server-Sent Events (SSE) 流式推送机制，大模型生成的回答逐字流式输出至前端，首字节响应时间 (TTFB) 从 2.5s 降至 0.8s
   - 相比传统的全量等待后返回，用户主观等待感知时间降低 60%，对话留存率提升显著

---

### 三、性能优化与高可用保障（主要负责）

**问题背景**: 高峰期 LLM API 调用频繁导致响应延迟高、成本上升，且相同问题的重复推理浪费计算资源。

**解决方案与实施**:

1. **Unified Cache 缓存体系**
   - 基于 Redis 实现三级缓存架构：**L1 热点缓存**（Caffeine 本地缓存，TTL 5min）→ **L2 分布式缓存**（Redis，TTL 30min）→ **L3 持久化缓存**（MySQL 冷数据）
   - 对常见病理问题（如"感冒吃什么药""发烧挂什么科"）的 LLM 标准化回答进行缓存，Key 为问题语义哈希（MD5 + 同义词归一化），避免相似问题重复调用大模型 API
   - 缓存命中率稳定在 45-55%，高峰期 LLM API 调用量减少 48%，月度 API 成本降低约 2000 元

2. **响应延迟优化**
   - 综合 SSE 流式输出 + Redis 缓存 + Pinecone 向量检索（亚秒级召回），全链路平均响应时间从优化前的 2.5s 降至 **0.35s**（缓存命中场景 50ms，缓存未命中场景 1.2s）
   - 通过 JMeter 压测验证，单服务器（4C8G）可稳定支撑 **80+ 并发会话**，系统可用性达 99.5%

3. **缓存一致性保障**
   - 设计缓存失效策略：医疗知识库更新时触发 Canal 监听 MySQL binlog，自动刷新 Redis 对应缓存 Key
   - 采用 Cache-Aside 模式，写操作先更新数据库再删除缓存，读操作先查缓存再回源，避免脏读

---

### 四、安全合规与部署交付（参与部分）

1. **数据安全与隐私保护**
   - 实现对话记忆隔离机制，每个用户独立会话窗口，会话数据物理隔离存储
   - 敏感信息（手机号、身份证号）采用 AES-256-GCM 加密存储，密钥托管于环境变量，数据库层仅存储密文
   - 所有 LLM 回答末尾强制附加免责声明："以上信息仅供参考，不能替代专业医疗诊断。如症状加重，请及时就医"

2. **Docker 容器化部署**
   - 编写各模块 Dockerfile（ consult / pathology / health-management / medical-resource ），基于 OpenJDK 17 轻量镜像构建
   - 编写 docker-compose.yml 编排全量服务（应用服务 + MySQL + MongoDB + Redis + Nginx 反向代理），实现一键启动开发/测试环境
   - 通过阿里云 ECS 完成生产环境部署，配合 Nginx 负载均衡实现 consult / pathology 模块的流量分发

3. **质量保障**
   - 编写 200 条医学问答自动化测试脚本（JUnit 5 + REST Assured），覆盖医院查询、疾病科普、政策咨询、边界 Case 四大场景
   - 建立 Bad Case 追踪机制，记录模型幻觉、检索失败、格式异常等问题，推动 3 轮迭代优化，核心问题修复率 85%
   - 参与回归测试，确保每次版本迭代后历史功能稳定性

---

## 实习成果总结

| 维度 | 关键指标 |
|:-----|:---------|
| **检索能力** | RAG 检索准确率 92%，混合检索召回率较单一策略提升 23% |
| **AI 能力** | 构建七层 Prompt 工程体系，幻觉率从 18% 降至 4% |
| **性能指标** | 平均响应时间 0.35s，单服务器支撑 80+ 并发，可用性 99.5% |
| **成本优化** | Redis 缓存命中率 45-55%，LLM API 调用量减少 48%，月度成本降低约 2000 元 |
| **交付范围** | 覆盖"智能伴诊-本地导诊-病理问答-健康管理"全场景，接入南昌 10+ 核心医院信息 |
| **工程规范** | 完成 Docker 容器化部署，编写 200 条自动化测试用例，Bad Case 修复率 85% |

---

## 技术亮点（面试可展开）

- **混合检索设计**: 为什么选择 RRF 而非线性加权？如何平衡向量检索的语义泛化能力与 BM25 的精确匹配能力？
- **意图路由**: 规则引擎 + 轻量模型的混合意图分类方案，如何在没有标注数据的情况下快速迭代？
- **缓存策略**: 为什么采用三级缓存而非单级 Redis？本地缓存与分布式缓存的权衡考量？
- **Prompt 安全**: 如何在提示词中嵌入安全护栏，防止模型输出违禁医疗建议？
- **LangChain4j 选型**: 相比直接使用 OpenAI SDK，LangChain4j 的抽象层带来了哪些开发效率提升？
